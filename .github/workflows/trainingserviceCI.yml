name: Training Service CI

on:
  push:
    paths:
      - 'TrainingService/**'
  pull_request:
    paths:
      - 'TrainingService/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        
      - name: Run build
        run: |
          echo "TrainingService building"