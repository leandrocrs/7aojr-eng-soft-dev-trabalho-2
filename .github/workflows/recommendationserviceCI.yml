name: Recommendation Service CI

on:
  push:
    paths:
      - 'RecommendationService/**'
  pull_request:
    paths:
      - 'RecommendationService/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        
      - name: Run Payment API build
        run: |
          echo "RecommendationService building"